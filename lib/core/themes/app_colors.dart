import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFFF472B6);
  static const Color primaryDark = Color(0xFFE85CA8);

  // Secondary
  static const Color secondary = Color(0xFFFBCFE8);

  // Accent
  static const Color accent = Color(0xFFC084FC);

  // Background
  static const Color background = Color(0xFFFFF7ED);

  // Surface
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF4A1D33);
  static const Color textSecondary = Color(0xFF7A4A60);

  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFB300);
  static const Color error = Color(0xFFE53935);

  // Neutral
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color grey = Color(0xFFBDBDBD);

  // Divider
  static const Color divider = Color(0xFFE9D7E3);
}